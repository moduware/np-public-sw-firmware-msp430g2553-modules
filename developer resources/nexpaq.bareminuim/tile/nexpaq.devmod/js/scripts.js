nexpaqAPI.setCurrentModule('DevMod');

// Page completely loaded
document.addEventListener("DOMContentLoaded", function (e) {
	//actions after loading the page
});

/*upstream
window.nexpaqAPI.DevMod.last_data.fun
*/

/*Downstream
window.nexpaqAPI.DevMod.send("2700", [1]);
*/
